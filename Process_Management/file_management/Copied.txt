MR. BENEDICT

What is SYstem Programming ?
-> Writing program that direcly interact with OS kernel

Syscall has 2 modes: User and Kernel Mode.

Syscall used by kernel and Libaries use syscall within themselves internally, but users use abstract functions.

Syscall has Higher COntext switches, Libraries use lower switchings.

process:
A program to execution - an independent unit with its memory, resources and execution state.
PID, PC, CPU Reg

Shells:
BASH - Bourne Again Shells
Zsh
Korn Shell
Csh


fork() processes - Ex. Orphan, child, Zombie processes.
exec() family
wait(&status)
-> prevents a process from becoming a Zombie.

cp one file to another using i/o systemcalls.

Lowlevel APIs
-> open()
-> read()
-> write()

open() flags and Modes
chmod and Permission related functions.
lseek() & Random Access.

File perm and Stat()

Directory ops:
opendir(path)
readdir(dir)
closedir(dir)
mkdir(path, mode)
rmdir(dir)

PATH=$PATH:.

I/O Operation:
Functions are Blocking and Raw syscalls Non-blocking IOs
select, poll, epoll()

Device IO & ioctl()
int ioctl(fd)

cut cmd in cli,
systemcall tracing experiment **

** Makefile creation: how to create a makefile

** Loadable Kernel Module using Makefile => basic Hello world first.



Afternoon Session:

1. Process Fundamentals.
Process contains -> PC, CPU registers, stack, heap, open file descirptors.
Threads are LWP (Light Weight processes)

Process States:
New, ready, running, Waited/Blocked and terminated.

PCB (Proc Ctrl Blocks):
PID, PPID.

Creating Process:
fork()
 Returns 0 in child, Returns Child's PID in Parent process, Returns -1 when fails.
 Replacings Process( exec family)

 Zombie and Orphan Process -> First program needed to be implemented by us.

 Reaping and Proces Table.

Signals & Advanced Management:
SIGKILL, SIGCHLD, etc...

IPC:
Pipes, Named Pipes (FIFO), Shared mem, Msg queues, Sockets.

Thread programming:::
Threads and Mutex Programs need to be Done.

Each thread has its own Stack, registers and PC,
Multithreading enables true concurency on Multicore systems.

POSIX threads are standard threads on LINUX / UNIX systems : pthreads()

Critcial Section -> Code that access shared resources and must not run concurrantly.
Soln: Mutex lock object is used to enforce this guarrantee.

Mutex Lifecycle:
1. pthread_mutex_init()
2. pthread_mutex_lock()
3. critical section runs.
4. pthread_mutex_unlock()
5. pthread_mutex_destroy()

Deadlocks:

